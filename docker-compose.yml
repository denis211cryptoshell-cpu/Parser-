version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: parser-bot-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

  bot:
    build: .
    container_name: parser-bot
    restart: unless-stopped
    depends_on:
      - redis
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

volumes:
  redis_data:
